 #include <stdio.h>
#include <math.h>

int main()
{

	float x, y;
	int i;
	for (i=0 ; i<100 ; i++)
	
	{
	if (x>=0.0)
		{

		printf("Please enter a value:");

		scanf("%f", &x);

		y=sqrt( x );

		printf("The value of sqrt of %f is %f  \n", x, y );
		}
	else
		{
		printf ("The calculation is not accepted. Please put a value >0.0");
		}
	
	}


}
